<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aloli.security.mapper.UserMapper">
 <!--  <resultMap id="studentMap" type="com.aloli.security.apiudent">
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="age" jdbcType="INTEGER" property="age" />
  </resultMap> -->

  <!-- <select id="listStudent"  resultType="Student">
    select * from student
  </select>
   <select id="getStudent"  resultType="Student">
     select * from student where name=#{name}
  </select>
  <select id="getStudent2"  resultType="Student">
     select * from student where name=#{name}
  </select>
    <select id="getStudent3"  resultType="Student">
     select * from student where name=#{name}
  </select>
  <select id="getStudent4"  resultType="Student">
     select * from student where name=#{name}
  </select>
  <update id="updateStudent"  parameterType="map" >

    update student set age=#{age} where name= #{name}
  </update>
  <delete id="deleteStudent" parameterType="java.lang.String">
    delete  from student where name= #{name}
  </delete> -->
</mapper>
